# 移动端 App Shell 重构：最终技术实施方案

> 目标读者：后续继续实施本项目的工程代理（包括 Gemini）
>
> 本文作为当前阶段唯一有效的实施基线。现有工作区中的实验性改动仅供参考，不构成最终实现标准；若与本文冲突，一律以本文为准。

---

## 1. 项目目标

本次重构不是“继续给 NexT 打补丁”，而是将当前 Hexo + NexT 静态站点改造成一个具备移动端 App Shell 体验的信息流站点，同时保留：

- 现有 Hexo + NexT + GitHub Pages 部署方式
- 现有 AI 日报内容生产规则与数据链路
- 现有非 digest 文章与内容目录结构

本次重构必须解决的核心问题：

- 移动端导航形态混乱，旧补丁式逻辑相互叠加
- 首页、日报页、笔记页的数据组织与分页机制不匹配
- Pjax 已开启，但自定义脚本与跳转方式不兼容
- GitHub Pages 子路径 `/julynotebook/` 下存在路径硬编码风险
- 详情页目录、卡片化结构、标题栏尚未形成统一实现

---

## 2. 不可妥协的实施原则

### 2.1 数据先于样式

首页、日报页、笔记页需要的展示数据，必须在 Hexo 构建阶段准备好；模板只负责渲染，前端只负责交互。

禁止继续采用以下做法：

- 依赖 `page.posts` 临时凑出首页 20 篇混合流
- 依赖分页后的分类页 DOM 再用 JS 现场 regroup
- 在 `.njk` 模板里写复杂 JS 表达式
- 在前端运行时从正文 DOM 里二次“猜摘要”

### 2.2 路由安全优先

站点部署根路径是 `/julynotebook/`。任何模板和脚本都不得假设站点运行在 `/` 根目录。

强制规则：

- 模板中所有内部链接统一使用 `url_for(...)`
- 前端脚本统一基于 `CONFIG.root` 做路径归一化
- 首页判断、返回 fallback、菜单高亮均不得写死 `/`

### 2.3 Pjax 兼容是硬约束

既然决定保留 NexT 的 Pjax 能力，就必须按 Pjax 的生命周期设计所有脚本。

强制规则：

- 所有导航跳转必须使用真实 `<a href="...">`
- 禁止继续使用 `onclick="window.location.href='...'"` 作为主要跳转方式
- 所有自定义脚本必须支持 `DOMContentLoaded` 与 `pjax:success` 双初始化
- 所有初始化函数必须幂等，避免重复插入 DOM 或重复绑定事件

### 2.4 Theme Fork 正式确认

继续沿用当前的本地主题 Fork 方案：

- 使用仓库内的 `themes/next`
- 不再依赖 npm 中的 `hexo-theme-next`
- 后续主题升级按“手工选择性回收上游改动”处理，不走自动升级

这是一个明确的架构决策，不再视为临时过渡手段。

---

## 3. 最终交互规则

### 3.1 首页

- 首页展示“最近 20 篇混合流”
- 内容来源包含：`每日资讯` 与 `july笔记`
- 顶部 Tab：`全部 / AI 日报 / AI 笔记`
- Tab 为前端单页筛选，不触发路由跳转
- 页面底部固定展示：
  - `查看全部日报 >`
  - `查看全部笔记 >`

说明：

- 首页不做“严格近 60 天”时间过滤，采用“最近 20 篇”策略
- 首页数据不再依赖默认分页机制

### 3.2 AI 日报列表页

- 按月份分组展示，格式为 `YYYY.MM`
- 最近 2 个月：显示完整日报卡片
- 早于 2 个月：降级为紧凑列表项，仅保留标题、日期、入口箭头

说明：

- “近 2 个月”的判断必须在数据层完成，不在模板中用临时计数器近似替代
- 紧凑列表项不再携带摘要，减轻 HTML 负载

### 3.3 AI 笔记列表页

- 显示完整笔记集合，不使用分页
- 顶部为横向可滚动标签栏
- 标签筛选为单选
- 默认标签为 `全部`

说明：

- 笔记页不再依赖分类页分页后的 `page.posts`
- 需要专门的数据视图输出完整笔记列表和标签列表

### 3.4 详情页

- 左上角：返回按钮
- 中间标题：当前页面名称或详情页名称
- 右上角：目录按钮
- 目录从右侧抽屉展开
- `重点资讯`、`其他快讯`、`核心论文` 以白底卡片呈现

适用范围：

- `AI 日报` 详情页：启用目录抽屉、区块卡片、摘要结构增强
- `AI 笔记` 详情页：若存在层级标题，则同样启用目录抽屉

---

## 4. 最终架构

## 4.1 构建期数据层

构建期统一产出 4 套视图数据，不再让模板自行拼装：

### A. `app_post_meta`

统一为所有文章产出：

- `app_type`
- `app_badge`
- `app_excerpt`
- `app_tags_str`
- `app_entry_count`
- `app_has_toc`

摘要优先级固定为：

1. Front Matter `app_excerpt`
2. `description`
3. `excerpt`
4. HTML fallback 抽取

### B. `latest_mixed_posts`

用于首页的最近 20 篇混合流。

必须：

- 全站文章按日期倒序
- 截取前 20 条
- 每条记录已带齐 `app_*` 字段

### C. `daily_archive_view`

用于 `AI 日报` 页。

每个分组对象至少包含：

- `month`
- `is_expanded`
- `posts`

每篇日报记录至少包含：

- `title`
- `path`
- `date`
- `app_excerpt`
- `app_entry_count`
- `is_compact`

### D. `notes_archive_view`

用于 `AI 笔记` 页。

至少包含：

- `tags`
- `posts`

每篇笔记记录至少包含：

- `title`
- `path`
- `date`
- `app_excerpt`
- `app_tags_str`

### 4.2 模板层

模板层只允许做：

- `for`
- `if`
- 基础字段输出
- `url_for(...)`

模板层禁止做：

- `map / reduce / filter`
- 字符串拼装型业务逻辑
- 内联脚本绑定主要交互
- 使用 `onclick` 充当路由

### 4.3 前端运行时层

前端运行时拆为 3 个模块：

#### `source/js/app-shell.js`

职责：

- 顶部 App Header
- 左侧菜单抽屉
- 遮罩与滚动锁定
- 返回逻辑
- 页面标题同步
- 路由判断
- `pjax:send` / `pjax:success` 生命周期管理

#### `source/js/app-views.js`

职责：

- 首页 Tab 筛选
- 笔记页标签筛选
- 筛选状态恢复
- 视图级幂等初始化

#### `source/js/post-layout.js`

职责：

- 详情页区块卡片增强
- 详情页目录抽屉接管
- 历史 digest 的“关键信号”压缩展示

说明：

- `post-layout.js` 只负责详情页，不再负责全站导航壳层

### 4.4 样式层

样式拆分为独立模块，不再继续堆叠到一个大文件：

- `source/css/app-shell.styl`
- `source/css/app-views.styl`
- `source/css/app-cards.styl`
- `source/css/detail-view.styl`

`source/_data/styles.styl` 只作为汇总入口。

---

## 5. 目录抽屉接管的正确方式

必须明确：

- NexT 的正文目录容器是 `.post-toc-wrap`
- `.site-overview-wrap` 是站点概览，不是正文目录

因此，目录抽屉的正确实现是：

- 从现有页面中的 `.post-toc-wrap` 读取目录内容
- 克隆或迁移其内部目录节点到 `.app-toc-drawer`
- 由 `app-shell.js` 统一控制打开、关闭、遮罩和滚动锁定

禁止继续把 `.site-overview-wrap` 当成 TOC 来源。

---

## 6. 文件级实施清单

### 6.1 需要新增或重构的构建脚本

- 新增或合并为：`scripts/app-post-meta.js`
- 保留并重构：`scripts/home-mixed-feed.js`
- 重命名并重构：`scripts/monthly-archive.js` -> `scripts/daily-archive-view.js`
- 新增：`scripts/notes-archive-view.js`
- 保留并收口：`scripts/post-structure.js`

说明：

- 当前实验性脚本 `compile-excerpt.js`、`notes-tags-string.js` 的职责应并入统一的数据层脚本，避免字段覆盖顺序耦合

### 6.2 需要重写的模板

- `themes/next/layout/index.njk`
- `themes/next/layout/category.njk`
- `themes/next/layout/post.njk`（仅在确有必要时）
- `themes/next/layout/_layout.njk`（仅在壳层结构必须调整时）

### 6.3 需要保留但重构的前端脚本

- `source/js/app-shell.js`
- `source/js/post-layout.js`

### 6.4 需要新增的前端脚本

- `source/js/app-views.js`

### 6.5 需要清理的文件/引用

- 从 `source/_data/body-end.njk` 中删除已失效引用
- 不再恢复 `source/js/home-tabs.js`
- 移除模板中的内联交互脚本
- 移除主要路径上的 `onclick="window.location.href"`

---

## 7. 分阶段实施顺序

## 阶段 0：先恢复构建稳定

目标：

- 当前工作区必须先回到 `hexo generate` 可通过状态

必须完成：

- 修复模板中的非法 Nunjucks 语法
- 删除失效脚本引用
- 清理显式 404 资源

退出条件：

- `npx hexo generate` 成功
- 浏览器控制台无新增基础错误

## 阶段 1：数据层成型

目标：

- 首页、日报页、笔记页全部改用预备数据视图

必须完成：

- 统一 `app_*` 元数据
- 首页最近 20 篇混合流
- 日报月分组视图
- 笔记完整数据视图

退出条件：

- 模板已不再依赖复杂表达式组装数据
- 首页/日报/笔记数据来源明确且稳定

## 阶段 2：模板层切换

目标：

- 用新数据视图替换旧模板渲染方式

必须完成：

- 首页模板接入 `latest_mixed_posts`
- 日报模板接入 `daily_archive_view`
- 笔记模板接入 `notes_archive_view`
- 所有卡片改成真实 `<a>`

退出条件：

- 站内主要导航已全部可被 Pjax 拦截
- 不再依赖 `page.posts` 满足业务页核心展示

## 阶段 3：App Shell 与 Pjax 生命周期

目标：

- 统一导航壳层

必须完成：

- 菜单抽屉
- 返回逻辑
- 标题切换
- 遮罩与滚动锁定
- `DOMContentLoaded + pjax:success` 幂等初始化
- `pjax:send` 时关闭抽屉

退出条件：

- 首页与内页导航形态切换稳定
- 返回后滚动位置保留

## 阶段 4：详情页结构增强

目标：

- 统一详情页阅读体验

必须完成：

- TOC 右侧抽屉
- 关键段落卡片化
- 历史 digest 信号压缩

退出条件：

- 日报与笔记详情页在移动端结构统一
- 无旧 sidebar 交互残留冲突

## 阶段 5：样式收口与清理

目标：

- 去掉旧补丁，形成稳定样式层

必须完成：

- 样式模块拆分
- 清理旧底部导航逻辑
- 清理冗余覆盖

退出条件：

- 移动端和桌面端都有明确展示边界
- 样式不再依赖大量临时 `!important` 才能工作

---

## 8. 验收标准

以下全部满足，才算本次重构完成：

- `npx hexo generate` 稳定通过
- GitHub Pages 子路径 `/julynotebook/` 下所有主入口跳转正常
- 首页稳定显示最近 20 篇混合流
- `AI 日报` 页按月分组，近 2 个月卡片、历史月份紧凑列表
- `AI 笔记` 页基于完整数据集做单选标签筛选
- 所有主导航使用真实 `<a>`，Pjax 生效
- 返回上页时滚动位置保留
- 详情页右上角目录抽屉稳定可用
- Console / Network 无 404、无重复绑定、无明显报错
- 不破坏现有 digest 内容生产链路与 GitHub Pages 部署链路

---

## 9. 补充边界约束

### 9.1 笔记标签筛选的扩容边界

当前方案明确采用：

- `AI 笔记` 页一次性输出完整笔记集合
- 前端通过单选标签对现有卡片做本地筛选

这在当前站点体量下是可接受方案，但必须明确其扩容边界：

- 短期内不为“未来几百篇笔记”的极端情况过度设计
- 当前阶段优先保证交互清晰、实现稳定、Pjax 兼容
- 当笔记规模显著增长、导致首屏 HTML 体积或 DOM 数量明显膨胀时，再进入下一阶段优化

届时允许的升级方向包括：

- 按月份或年份分段输出笔记集合
- 默认折叠历史分组，仅展开最近内容
- 采用更轻量的列表节点结构，减少单卡片 DOM 层级

当前阶段禁止因为“远期规模焦虑”回退到分页片段筛选方案。

### 9.2 SEO 与 Canonical 约束

首页改为 `latest_mixed_posts` 视图、日报页改为“月分组总览页”之后，必须同步处理原有分页语义，避免留下重复内容入口。

强制规则：

- 自定义首页和自定义日报页必须有明确的主语义 URL
- 若保留原始分页物理页，必须明确其 canonical 指向主列表页
- 对已失去独立内容价值的分页页，优先考虑 `noindex,follow` 或直接回归主列表语义
- 不允许既保留旧分页壳页面，又让其与新总览页在搜索引擎层面形成重复竞态

说明：

- Sitemap 只能帮助爬虫发现页面，不能替代 canonical 语义治理
- 这不是当前移动端重构的主目标，但必须作为实现约束写入方案

---

## 10. 明确的禁区

以下做法本轮禁止再出现：

- 继续在模板内写复杂 JS 表达式
- 继续使用 `onclick="window.location.href"` 作为主要导航
- 继续依赖分页后的 `page.posts` 充当完整业务数据集
- 把 `.site-overview-wrap` 当成 TOC 数据源
- 把首页 20 篇混合流重新退回默认分页逻辑
- 把笔记页筛选建立在“当前分页片段”而非完整笔记集之上
- 在未解决构建错误之前继续扩展视觉层改动

---

## 11. 实施口径

后续无论由谁继续开发，均按以下口径执行：

- 先恢复构建，再重建数据层，再改模板，再接壳层，再做详情页
- 任何偏离本文的实现方案，都需要先回到评审阶段重新确认
- 若当前实验性代码与本文冲突，可直接重构或删除；实验代码不是保留目标

本文即为下一轮实施的正式参考基线。
