---
title: {{ title }}
date: {{ date }}
categories: [july笔记]
tags:
---

> 在正文中直接写 `#标签`（例如：#AI #学习 #工作流），构建时会自动加入文章 tags。
> 图片建议直接写 `![](图片名.png)`：把图片放到和本文同名的资源目录即可（已启用 `post_asset_folder`）。
> 需要富文本效果时，可直接在 Markdown 里混写 HTML（如 `<details>...</details>`、`<span style="color:#ff6b6b">高亮</span>`）。

在这里开始写你的学习整理内容。
